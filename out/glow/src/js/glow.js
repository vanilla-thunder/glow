$( function () {

    // allgemeines Zeug
    $( "img" ).unveil();
    $( '.hasTooltip' ).tooltip({container: 'body'});
    $( '.hasPopover' ).popover();


    
    // Variantenauswahl

    

});