$( function () {

    // allgemeines Zeug
    // Lazy Loading für Bilder
    $( "img" ).unveil();

    // Globale Tooltip-Klasse
    $( '.hasTooltip' ).tooltip();

    // Globale PopOver-Klasse
    $( '.hasPopover' ).popover();


    
    // Variantenauswahl

    

});